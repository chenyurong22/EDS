/*
 * =============================================================================
 * Xaloqi EDS
 * FILE: GENERATED — generated/did_safety_wrappers.c
 *
 * ECU       : BMS_MainController
 * Version   : 1.0.0
 * Generated : 2026-05-20T07:21:48Z
 * ASIL Level: B
 *
 * PURPOSE: Per-DID safe accessor implementations (ASIL-B).
 *
 *          Architecture:
 *            diagnostics_config.yaml (dids section)
 *              -> tools/codegen.py (build_did_safety_wrappers_context)
 *                -> tools/templates/did_safety_wrappers.c.j2
 *                  -> generated/did_safety_wrappers.c  (this file)
 *
 * WARNING: DO NOT EDIT MANUALLY.
 *          Regenerate: python3 tools/codegen.py --config <yaml>
 *                                               --out generated/
 *                                               --safety-wrappers
 *
 * SAFETY  : Each function:
 *   1. NULL-checks all pointer arguments (REQ-SAFE-004).
 *   2. Resolves the DID in the database (REQ-SAFE-001).
 *   3. Validates session + security access (REQ-SAFE-002/003).
 *   4. Checks buffer length against DID data_length (REQ-SAFE-006).
 *   5. Invokes the DID read/write callback.
 * STANDARD: MISRA C:2012 alignment intended.
 * =============================================================================
 */

/* AUTOGENERATED - DO NOT EDIT */

#include "did_safety_wrappers.h"
#include "did_handlers.h"

/* ================================================================
 * Per-DID safe accessor implementations (ASIL-B)
 * ================================================================ */

/* --------------------------------------------------------------------------
 * Safe read: DID 0xF190 — VehicleIdentificationNumber
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_vehicleidentificationnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)61840U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_vehicleidentificationnumber(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xF18C — ECUSerialNumber
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_ecuserialnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)61836U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_ecuserialnumber(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xF187 — VehicleManufacturerSparePartNumber
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_vehiclemanufacturersparepartnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)61831U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_vehiclemanufacturersparepartnumber(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xF189 — ECUSoftwareVersionNumber
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_ecusoftwareversionnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)61833U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_ecusoftwareversionnumber(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDB00 — BMS_PackVoltage_mV
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_packvoltage_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56064U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_packvoltage_mv(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDB01 — BMS_PackCurrent_100mA
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_packcurrent_100ma(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56065U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_packcurrent_100ma(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDB02 — BMS_PackPower_10W
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_packpower_10w(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56066U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_packpower_10w(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDB03 — BMS_ContactorState
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_contactorstate(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56067U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_contactorstate(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDB04 — BMS_InsulationResistance_kOhm
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_insulationresistance_kohm(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56068U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_insulationresistance_kohm(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDC01 — BMS_CellGroup1_Voltage_mV
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_cellgroup1_voltage_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56321U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_cellgroup1_voltage_mv(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDC02 — BMS_CellGroup2_Voltage_mV
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_cellgroup2_voltage_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56322U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_cellgroup2_voltage_mv(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDC03 — BMS_CellGroup3_Voltage_mV
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_cellgroup3_voltage_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56323U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_cellgroup3_voltage_mv(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDC04 — BMS_CellGroup4_Voltage_mV
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_cellgroup4_voltage_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56324U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_cellgroup4_voltage_mv(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDD00 — BMS_MaxCellTemperature_degC
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_maxcelltemperature_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56576U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_maxcelltemperature_degc(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDD01 — BMS_MinCellTemperature_degC
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_mincelltemperature_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56577U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_mincelltemperature_degc(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDD02 — BMS_CoolantInletTemperature_degC
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_coolantinlettemperature_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56578U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_coolantinlettemperature_degc(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDD03 — BMS_BMSBoardTemperature_degC
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_bmsboardtemperature_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56579U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_bmsboardtemperature_degc(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDE00 — BMS_StateOfCharge_04pct
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_stateofcharge_04pct(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56832U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_stateofcharge_04pct(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDE01 — BMS_StateOfHealth_04pct
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_stateofhealth_04pct(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56833U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_stateofhealth_04pct(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xDE02 — BMS_BalancingStateBitmask
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_balancingstatebitmask(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)56834U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_balancingstatebitmask(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xD900 — BMS_FaultFlagsBitmask
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_faultflagsbitmask(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)55552U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_faultflagsbitmask(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xD901 — BMS_FullChargeCapacity_Wh
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_fullchargecapacity_wh(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)55553U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_fullchargecapacity_wh(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xD910 — BMS_OVThreshold_mV
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_ovthreshold_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)55568U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_ovthreshold_mv(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xD911 — BMS_UVThreshold_mV
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_bms_uvthreshold_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)55569U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_bms_uvthreshold_mv(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe write: DID 0xF187 — VehicleManufacturerSparePartNumber
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_write_vehiclemanufacturersparepartnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)61831U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_WRITE));

    /* Step 4 — Write length check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_write_data_length(entry, len));

    /* Step 5 — Invoke DID write callback */
    return did_write_vehiclemanufacturersparepartnumber(buf, len);
}

/* --------------------------------------------------------------------------
 * Safe write: DID 0xD910 — BMS_OVThreshold_mV
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_write_bms_ovthreshold_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)55568U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_WRITE));

    /* Step 4 — Write length check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_write_data_length(entry, len));

    /* Step 5 — Invoke DID write callback */
    return did_write_bms_ovthreshold_mv(buf, len);
}

/* --------------------------------------------------------------------------
 * Safe write: DID 0xD911 — BMS_UVThreshold_mV
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_write_bms_uvthreshold_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)55569U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_WRITE));

    /* Step 4 — Write length check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_write_data_length(entry, len));

    /* Step 5 — Invoke DID write callback */
    return did_write_bms_uvthreshold_mv(buf, len);
}
