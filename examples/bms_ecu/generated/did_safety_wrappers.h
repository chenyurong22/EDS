/*
 * =============================================================================
 * Xaloqi EDS
 * FILE: GENERATED — generated/did_safety_wrappers.h
 *
 * ECU       : BMS_MainController
 * Version   : 1.0.0
 * Generated : 2026-05-20T07:21:48Z
 * ASIL Level: B
 *
 * PURPOSE: Declarations for per-DID safe accessor wrapper functions.
 *          Each function enforces the full ASIL-B validation chain before
 *          invoking the underlying DID read/write callback.
 *
 *          Architecture:
 *            diagnostics_config.yaml (dids section)
 *              -> tools/codegen.py (build_did_safety_wrappers_context)
 *                -> tools/templates/did_safety_wrappers.h.j2
 *                  -> generated/did_safety_wrappers.h  (this file)
 *
 *          This design allows service handlers (service_0x22.c, service_0x2E.c)
 *          to call a single safe accessor per DID rather than assembling the
 *          5-step validation chain inline in each handler.
 *
 * WARNING: DO NOT EDIT MANUALLY.
 *          Regenerate: python3 tools/codegen.py --config <yaml>
 *                                               --out generated/
 *                                               --safety-wrappers
 *
 * SAFETY  : All wrappers enforce the mandatory ASIL-B check sequence:
 *           1. NULL-checks all pointer arguments        (REQ-SAFE-004)
 *           2. Resolves DID in the database             (REQ-SAFE-001)
 *           3. Validates session + security access      (REQ-SAFE-002/003)
 *           4. Checks buffer length vs DID data_length  (REQ-SAFE-006)
 *           5. Invokes the DID read/write callback
 * STANDARD: MISRA C:2012 alignment intended.
 * =============================================================================
 */

/* AUTOGENERATED - DO NOT EDIT */

#ifndef DID_SAFETY_WRAPPERS_H
#define DID_SAFETY_WRAPPERS_H

#include "uds_types.h"
#include "uds_safety.h"
#include "did_database.h"

#ifdef __cplusplus
extern "C" {
#endif

/* ================================================================
 * Per-DID safe accessor declarations (ASIL-B)
 *
 * Each wrapper performs NULL checks, session, security, and
 * bounds validation before invoking the DID callback.
 * ================================================================ */

/**
 * @brief Safe read accessor for DID 0xF190 — VehicleIdentificationNumber.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xF190 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 17 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 17).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_vehicleidentificationnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xF18C — ECUSerialNumber.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xF18C exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 8 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 8).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_ecuserialnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xF187 — VehicleManufacturerSparePartNumber.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xF187 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 11 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 11).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_vehiclemanufacturersparepartnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xF189 — ECUSoftwareVersionNumber.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xF189 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 4 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 4).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_ecusoftwareversionnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDB00 — BMS_PackVoltage_mV.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDB00 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 4 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 4).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_packvoltage_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDB01 — BMS_PackCurrent_100mA.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDB01 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_packcurrent_100ma(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDB02 — BMS_PackPower_10W.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDB02 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_packpower_10w(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDB03 — BMS_ContactorState.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDB03 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_contactorstate(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDB04 — BMS_InsulationResistance_kOhm.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDB04 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_insulationresistance_kohm(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDC01 — BMS_CellGroup1_Voltage_mV.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDC01 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_cellgroup1_voltage_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDC02 — BMS_CellGroup2_Voltage_mV.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDC02 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_cellgroup2_voltage_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDC03 — BMS_CellGroup3_Voltage_mV.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDC03 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_cellgroup3_voltage_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDC04 — BMS_CellGroup4_Voltage_mV.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDC04 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_cellgroup4_voltage_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDD00 — BMS_MaxCellTemperature_degC.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDD00 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_maxcelltemperature_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDD01 — BMS_MinCellTemperature_degC.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDD01 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_mincelltemperature_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDD02 — BMS_CoolantInletTemperature_degC.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDD02 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_coolantinlettemperature_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDD03 — BMS_BMSBoardTemperature_degC.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDD03 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_bmsboardtemperature_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDE00 — BMS_StateOfCharge_04pct.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDE00 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_stateofcharge_04pct(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDE01 — BMS_StateOfHealth_04pct.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDE01 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_stateofhealth_04pct(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xDE02 — BMS_BalancingStateBitmask.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xDE02 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_balancingstatebitmask(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xD900 — BMS_FaultFlagsBitmask.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xD900 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_faultflagsbitmask(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xD901 — BMS_FullChargeCapacity_Wh.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xD901 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 4 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 4).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_fullchargecapacity_wh(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xD910 — BMS_OVThreshold_mV.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xD910 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_ovthreshold_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xD911 — BMS_UVThreshold_mV.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xD911 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_bms_uvthreshold_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe write accessor for DID 0xF187 — VehicleManufacturerSparePartNumber.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xF187 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 1  (REQ-SAFE-003)
 *   - len == 11 bytes exactly  (REQ-SAFE-006)
 *
 * @param[in] session_ctx   Active UDS session context.
 * @param[in] security_ctx  Active UDS security context.
 * @param[in] buf           Buffer containing new DID data (11 byte(s)).
 * @param[in] len           Number of bytes in buf.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_write_vehiclemanufacturersparepartnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len
);

/**
 * @brief Safe write accessor for DID 0xD910 — BMS_OVThreshold_mV.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xD910 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 1  (REQ-SAFE-003)
 *   - len == 2 bytes exactly  (REQ-SAFE-006)
 *
 * @param[in] session_ctx   Active UDS session context.
 * @param[in] security_ctx  Active UDS security context.
 * @param[in] buf           Buffer containing new DID data (2 byte(s)).
 * @param[in] len           Number of bytes in buf.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_write_bms_ovthreshold_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len
);

/**
 * @brief Safe write accessor for DID 0xD911 — BMS_UVThreshold_mV.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xD911 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 1  (REQ-SAFE-003)
 *   - len == 2 bytes exactly  (REQ-SAFE-006)
 *
 * @param[in] session_ctx   Active UDS session context.
 * @param[in] security_ctx  Active UDS security context.
 * @param[in] buf           Buffer containing new DID data (2 byte(s)).
 * @param[in] len           Number of bytes in buf.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_write_bms_uvthreshold_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len
);

#ifdef __cplusplus
}
#endif

#endif /* DID_SAFETY_WRAPPERS_H */
