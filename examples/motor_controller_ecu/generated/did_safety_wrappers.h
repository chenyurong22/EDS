/*
 * =============================================================================
 * Xaloqi EDS
 * FILE: GENERATED — generated/did_safety_wrappers.h
 *
 * ECU       : MotorController_Inverter
 * Version   : 1.0.0
 * Generated : 2026-05-20T07:21:48Z
 * ASIL Level: B
 *
 * PURPOSE: Declarations for per-DID safe accessor wrapper functions.
 *          Each function enforces the full ASIL-B validation chain before
 *          invoking the underlying DID read/write callback.
 *
 *          Architecture:
 *            diagnostics_config.yaml (dids section)
 *              -> tools/codegen.py (build_did_safety_wrappers_context)
 *                -> tools/templates/did_safety_wrappers.h.j2
 *                  -> generated/did_safety_wrappers.h  (this file)
 *
 *          This design allows service handlers (service_0x22.c, service_0x2E.c)
 *          to call a single safe accessor per DID rather than assembling the
 *          5-step validation chain inline in each handler.
 *
 * WARNING: DO NOT EDIT MANUALLY.
 *          Regenerate: python3 tools/codegen.py --config <yaml>
 *                                               --out generated/
 *                                               --safety-wrappers
 *
 * SAFETY  : All wrappers enforce the mandatory ASIL-B check sequence:
 *           1. NULL-checks all pointer arguments        (REQ-SAFE-004)
 *           2. Resolves DID in the database             (REQ-SAFE-001)
 *           3. Validates session + security access      (REQ-SAFE-002/003)
 *           4. Checks buffer length vs DID data_length  (REQ-SAFE-006)
 *           5. Invokes the DID read/write callback
 * STANDARD: MISRA C:2012 alignment intended.
 * =============================================================================
 */

/* AUTOGENERATED - DO NOT EDIT */

#ifndef DID_SAFETY_WRAPPERS_H
#define DID_SAFETY_WRAPPERS_H

#include "uds_types.h"
#include "uds_safety.h"
#include "did_database.h"

#ifdef __cplusplus
extern "C" {
#endif

/* ================================================================
 * Per-DID safe accessor declarations (ASIL-B)
 *
 * Each wrapper performs NULL checks, session, security, and
 * bounds validation before invoking the DID callback.
 * ================================================================ */

/**
 * @brief Safe read accessor for DID 0xF190 — VehicleIdentificationNumber.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xF190 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 17 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 17).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_vehicleidentificationnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xF18C — ECUSerialNumber.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xF18C exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 8 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 8).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_ecuserialnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xF187 — VehicleManufacturerSparePartNumber.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xF187 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 11 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 11).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_vehiclemanufacturersparepartnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xF189 — ECUSoftwareVersionNumber.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xF189 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 4 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 4).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_ecusoftwareversionnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE001 — MC_RotorSpeed_rpm.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE001 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_rotorspeed_rpm(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE002 — MC_TorqueDemand_01Nm.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE002 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_torquedemand_01nm(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE003 — MC_TorqueActual_01Nm.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE003 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_torqueactual_01nm(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE004 — MC_RotorElectricalAngle_raw.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE004 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_rotorelectricalangle_raw(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE005 — MC_MechanicalPower_10W.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE005 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_mechanicalpower_10w(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE101 — MC_PhaseA_Current_100mA.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE101 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_phasea_current_100ma(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE102 — MC_PhaseB_Current_100mA.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE102 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_phaseb_current_100ma(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE103 — MC_PhaseC_Current_100mA.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE103 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_phasec_current_100ma(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE104 — MC_Iq_Current_100mA.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE104 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_iq_current_100ma(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE105 — MC_Id_Current_100mA.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE105 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_id_current_100ma(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE201 — MC_DCLink_Voltage_mV.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE201 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 4 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 4).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_dclink_voltage_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE202 — MC_DCLink_Current_100mA.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE202 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_dclink_current_100ma(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE203 — MC_ModulationIndex_04pct.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE203 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_modulationindex_04pct(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE301 — MC_IGBT_PhaseA_JunctionTemp_degC.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE301 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_igbt_phasea_junctiontemp_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE302 — MC_IGBT_PhaseB_JunctionTemp_degC.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE302 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_igbt_phaseb_junctiontemp_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE303 — MC_IGBT_PhaseC_JunctionTemp_degC.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE303 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_igbt_phasec_junctiontemp_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE304 — MC_MotorStatorTemp_degC.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE304 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_motorstatortemp_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE305 — MC_CoolantInletTemp_degC.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE305 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_coolantinlettemp_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE401 — MC_FaultStatusBitmask.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE401 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_faultstatusbitmask(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE402 — MC_OperatingMode.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE402 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_operatingmode(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE501 — MC_ResolverOffset_raw.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE501 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_resolveroffset_raw(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE502 — MC_TorqueScalingFactor_01pct.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE502 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_torquescalingfactor_01pct(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xE503 — MC_CurrentSensorTrim_100uA.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE503 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_mc_currentsensortrim_100ua(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe write accessor for DID 0xF187 — VehicleManufacturerSparePartNumber.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xF187 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 1  (REQ-SAFE-003)
 *   - len == 11 bytes exactly  (REQ-SAFE-006)
 *
 * @param[in] session_ctx   Active UDS session context.
 * @param[in] security_ctx  Active UDS security context.
 * @param[in] buf           Buffer containing new DID data (11 byte(s)).
 * @param[in] len           Number of bytes in buf.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_write_vehiclemanufacturersparepartnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len
);

/**
 * @brief Safe write accessor for DID 0xE501 — MC_ResolverOffset_raw.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE501 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 1  (REQ-SAFE-003)
 *   - len == 2 bytes exactly  (REQ-SAFE-006)
 *
 * @param[in] session_ctx   Active UDS session context.
 * @param[in] security_ctx  Active UDS security context.
 * @param[in] buf           Buffer containing new DID data (2 byte(s)).
 * @param[in] len           Number of bytes in buf.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_write_mc_resolveroffset_raw(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len
);

/**
 * @brief Safe write accessor for DID 0xE502 — MC_TorqueScalingFactor_01pct.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE502 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 1  (REQ-SAFE-003)
 *   - len == 2 bytes exactly  (REQ-SAFE-006)
 *
 * @param[in] session_ctx   Active UDS session context.
 * @param[in] security_ctx  Active UDS security context.
 * @param[in] buf           Buffer containing new DID data (2 byte(s)).
 * @param[in] len           Number of bytes in buf.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_write_mc_torquescalingfactor_01pct(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len
);

/**
 * @brief Safe write accessor for DID 0xE503 — MC_CurrentSensorTrim_100uA.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xE503 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 1  (REQ-SAFE-003)
 *   - len == 2 bytes exactly  (REQ-SAFE-006)
 *
 * @param[in] session_ctx   Active UDS session context.
 * @param[in] security_ctx  Active UDS security context.
 * @param[in] buf           Buffer containing new DID data (2 byte(s)).
 * @param[in] len           Number of bytes in buf.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_write_mc_currentsensortrim_100ua(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len
);

#ifdef __cplusplus
}
#endif

#endif /* DID_SAFETY_WRAPPERS_H */
