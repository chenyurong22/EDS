/*
 * =============================================================================
 * Xaloqi EDS
 * FILE: GENERATED — generated/did_safety_wrappers.c
 *
 * ECU       : MotorController_Inverter
 * Version   : 1.0.0
 * Generated : 2026-05-20T07:21:48Z
 * ASIL Level: B
 *
 * PURPOSE: Per-DID safe accessor implementations (ASIL-B).
 *
 *          Architecture:
 *            diagnostics_config.yaml (dids section)
 *              -> tools/codegen.py (build_did_safety_wrappers_context)
 *                -> tools/templates/did_safety_wrappers.c.j2
 *                  -> generated/did_safety_wrappers.c  (this file)
 *
 * WARNING: DO NOT EDIT MANUALLY.
 *          Regenerate: python3 tools/codegen.py --config <yaml>
 *                                               --out generated/
 *                                               --safety-wrappers
 *
 * SAFETY  : Each function:
 *   1. NULL-checks all pointer arguments (REQ-SAFE-004).
 *   2. Resolves the DID in the database (REQ-SAFE-001).
 *   3. Validates session + security access (REQ-SAFE-002/003).
 *   4. Checks buffer length against DID data_length (REQ-SAFE-006).
 *   5. Invokes the DID read/write callback.
 * STANDARD: MISRA C:2012 alignment intended.
 * =============================================================================
 */

/* AUTOGENERATED - DO NOT EDIT */

#include "did_safety_wrappers.h"
#include "did_handlers.h"

/* ================================================================
 * Per-DID safe accessor implementations (ASIL-B)
 * ================================================================ */

/* --------------------------------------------------------------------------
 * Safe read: DID 0xF190 — VehicleIdentificationNumber
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_vehicleidentificationnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)61840U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_vehicleidentificationnumber(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xF18C — ECUSerialNumber
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_ecuserialnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)61836U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_ecuserialnumber(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xF187 — VehicleManufacturerSparePartNumber
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_vehiclemanufacturersparepartnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)61831U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_vehiclemanufacturersparepartnumber(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xF189 — ECUSoftwareVersionNumber
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_ecusoftwareversionnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)61833U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_ecusoftwareversionnumber(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE001 — MC_RotorSpeed_rpm
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_rotorspeed_rpm(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)57345U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_rotorspeed_rpm(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE002 — MC_TorqueDemand_01Nm
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_torquedemand_01nm(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)57346U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_torquedemand_01nm(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE003 — MC_TorqueActual_01Nm
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_torqueactual_01nm(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)57347U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_torqueactual_01nm(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE004 — MC_RotorElectricalAngle_raw
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_rotorelectricalangle_raw(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)57348U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_rotorelectricalangle_raw(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE005 — MC_MechanicalPower_10W
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_mechanicalpower_10w(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)57349U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_mechanicalpower_10w(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE101 — MC_PhaseA_Current_100mA
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_phasea_current_100ma(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)57601U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_phasea_current_100ma(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE102 — MC_PhaseB_Current_100mA
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_phaseb_current_100ma(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)57602U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_phaseb_current_100ma(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE103 — MC_PhaseC_Current_100mA
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_phasec_current_100ma(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)57603U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_phasec_current_100ma(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE104 — MC_Iq_Current_100mA
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_iq_current_100ma(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)57604U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_iq_current_100ma(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE105 — MC_Id_Current_100mA
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_id_current_100ma(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)57605U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_id_current_100ma(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE201 — MC_DCLink_Voltage_mV
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_dclink_voltage_mv(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)57857U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_dclink_voltage_mv(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE202 — MC_DCLink_Current_100mA
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_dclink_current_100ma(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)57858U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_dclink_current_100ma(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE203 — MC_ModulationIndex_04pct
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_modulationindex_04pct(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)57859U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_modulationindex_04pct(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE301 — MC_IGBT_PhaseA_JunctionTemp_degC
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_igbt_phasea_junctiontemp_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)58113U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_igbt_phasea_junctiontemp_degc(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE302 — MC_IGBT_PhaseB_JunctionTemp_degC
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_igbt_phaseb_junctiontemp_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)58114U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_igbt_phaseb_junctiontemp_degc(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE303 — MC_IGBT_PhaseC_JunctionTemp_degC
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_igbt_phasec_junctiontemp_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)58115U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_igbt_phasec_junctiontemp_degc(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE304 — MC_MotorStatorTemp_degC
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_motorstatortemp_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)58116U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_motorstatortemp_degc(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE305 — MC_CoolantInletTemp_degC
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_coolantinlettemp_degc(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)58117U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_coolantinlettemp_degc(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE401 — MC_FaultStatusBitmask
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_faultstatusbitmask(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)58369U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_faultstatusbitmask(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE402 — MC_OperatingMode
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_operatingmode(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)58370U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_operatingmode(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE501 — MC_ResolverOffset_raw
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_resolveroffset_raw(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)58625U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_resolveroffset_raw(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE502 — MC_TorqueScalingFactor_01pct
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_torquescalingfactor_01pct(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)58626U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_torquescalingfactor_01pct(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xE503 — MC_CurrentSensorTrim_100uA
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_mc_currentsensortrim_100ua(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)58627U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_mc_currentsensortrim_100ua(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe write: DID 0xF187 — VehicleManufacturerSparePartNumber
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_write_vehiclemanufacturersparepartnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)61831U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_WRITE));

    /* Step 4 — Write length check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_write_data_length(entry, len));

    /* Step 5 — Invoke DID write callback */
    return did_write_vehiclemanufacturersparepartnumber(buf, len);
}

/* --------------------------------------------------------------------------
 * Safe write: DID 0xE501 — MC_ResolverOffset_raw
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_write_mc_resolveroffset_raw(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)58625U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_WRITE));

    /* Step 4 — Write length check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_write_data_length(entry, len));

    /* Step 5 — Invoke DID write callback */
    return did_write_mc_resolveroffset_raw(buf, len);
}

/* --------------------------------------------------------------------------
 * Safe write: DID 0xE502 — MC_TorqueScalingFactor_01pct
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_write_mc_torquescalingfactor_01pct(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)58626U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_WRITE));

    /* Step 4 — Write length check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_write_data_length(entry, len));

    /* Step 5 — Invoke DID write callback */
    return did_write_mc_torquescalingfactor_01pct(buf, len);
}

/* --------------------------------------------------------------------------
 * Safe write: DID 0xE503 — MC_CurrentSensorTrim_100uA
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_write_mc_currentsensortrim_100ua(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)58627U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_WRITE));

    /* Step 4 — Write length check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_write_data_length(entry, len));

    /* Step 5 — Invoke DID write callback */
    return did_write_mc_currentsensortrim_100ua(buf, len);
}
