# examples/sensor_ecu/prj.conf
# Base configuration for all board targets.
# Board-specific overrides in boards/<board>/<board>.conf

# --- CAN driver ---
CONFIG_CAN=y
CONFIG_CAN_INIT_PRIORITY=80
CONFIG_STATS=y
CONFIG_CAN_STATS=y

# --- Sensor subsystem ---
CONFIG_SENSOR=y
CONFIG_I2C=y

# --- Kernel / threading ---
CONFIG_MULTITHREADING=y
CONFIG_THREAD_STACK_INFO=y
CONFIG_THREAD_NAME=y

# --- Tick rate: 1 ms for ISO-TP ---
CONFIG_SYS_CLOCK_TICKS_PER_SEC=1000
CONFIG_TICKLESS_KERNEL=n

# --- Watchdog ---
CONFIG_WATCHDOG=y
CONFIG_WDT_DISABLE_AT_BOOT=n
CONFIG_DIAG_WDT_WINDOW_MS=100

# --- Logging ---
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3
CONFIG_LOG_BACKEND_UART=y
CONFIG_LOG_BUFFER_SIZE=2048

# --- Safety hardening ---
CONFIG_ASSERT=y
CONFIG_STACK_CANARIES=y
CONFIG_STACK_SENTINEL=y

# --- No heap (MISRA-21.3) ---
CONFIG_HEAP_MEM_POOL_SIZE=0

# --- C library ---
CONFIG_NEWLIB_LIBC=y

# --- UDS stack parameters ---
CONFIG_UDS_SESSION_S3_TIMEOUT_MS=5000
CONFIG_ISOTP_TIMEOUT_CR_MS=150
CONFIG_ISOTP_TIMEOUT_BS_MS=75
CONFIG_ISOTP_TIMEOUT_AS_MS=25

# --- Task stack sizes ---
CONFIG_DIAG_TASK_STACK_SIZE=4096
CONFIG_DIAG_TASK_PRIORITY=5

# --- Build output ---
CONFIG_BUILD_OUTPUT_BIN=y
CONFIG_BUILD_OUTPUT_HEX=y
