/*
 * =============================================================================
 * Xaloqi EDS
 * FILE: GENERATED — generated/did_safety_wrappers.c
 *
 * ECU       : SensorECU
 * Version   : 1.0.0
 * Generated : 2026-06-23T19:16:14Z
 * ASIL Level: B
 *
 * PURPOSE: Per-DID safe accessor implementations (ASIL-B).
 *
 *          Architecture:
 *            diagnostics_config.yaml (dids section)
 *              -> tools/codegen.py (build_did_safety_wrappers_context)
 *                -> tools/templates/did_safety_wrappers.c.j2
 *                  -> generated/did_safety_wrappers.c  (this file)
 *
 * WARNING: DO NOT EDIT MANUALLY.
 *          Regenerate: python3 tools/codegen.py --config <yaml>
 *                                               --out generated/
 *                                               --safety-wrappers
 *
 * SAFETY  : Each function:
 *   1. NULL-checks all pointer arguments (REQ-SAFE-004).
 *   2. Resolves the DID in the database (REQ-SAFE-001).
 *   3. Validates session + security access (REQ-SAFE-002/003).
 *   4. Checks buffer length against DID data_length (REQ-SAFE-006).
 *   5. Invokes the DID read/write callback.
 * STANDARD: MISRA C:2012 alignment intended.
 * =============================================================================
 */

/* AUTOGENERATED - DO NOT EDIT */

#include "did_safety_wrappers.h"
#include "did_handlers.h"

/* ================================================================
 * Per-DID safe accessor implementations (ASIL-B)
 * ================================================================ */

/* --------------------------------------------------------------------------
 * Safe read: DID 0xF190 — VehicleIdentificationNumber
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_vehicleidentificationnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)61840U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_vehicleidentificationnumber(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xF18C — ECUSerialNumber
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_ecuserialnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)61836U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_ecuserialnumber(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xD001 — AmbientTemperature
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_ambienttemperature(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)53249U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_ambienttemperature(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xD002 — SupplyVoltage
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_supplyvoltage(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)53250U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_supplyvoltage(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xD003 — SensorStatusBitmask
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_sensorstatusbitmask(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)53251U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_sensorstatusbitmask(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xD010 — TemperatureThresholdHigh
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_temperaturethresholdhigh(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)53264U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_temperaturethresholdhigh(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xD011 — TemperatureThresholdLow
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_temperaturethresholdlow(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)53265U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_temperaturethresholdlow(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe write: DID 0xD010 — TemperatureThresholdHigh
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_write_temperaturethresholdhigh(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)53264U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_WRITE));

    /* Step 4 — Write length check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_write_data_length(entry, len));

    /* Step 5 — Invoke DID write callback */
    return did_write_temperaturethresholdhigh(buf, len);
}

/* --------------------------------------------------------------------------
 * Safe write: DID 0xD011 — TemperatureThresholdLow
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_write_temperaturethresholdlow(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)53265U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_WRITE));

    /* Step 4 — Write length check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_write_data_length(entry, len));

    /* Step 5 — Invoke DID write callback */
    return did_write_temperaturethresholdlow(buf, len);
}
