# =============================================================================
# GENERATED — pip requirements for running BasicECU integration tests
# =============================================================================
#
# xaloqi-tester replaces the inline ISO-TP/AES-CMAC/simulator implementation
# that was previously embedded in conftest.py.  Install it from your TestLab
# ZIP or via pip if you have a TestLab license:
#
#   pip install xaloqi-tester
#   OR
#   pip install -e /path/to/TestLab
#
xaloqi-tester>=1.0.0
pytest>=7.0
python-can>=4.0
pycryptodome>=3.18   # fallback if xaloqi-tester is not installed
