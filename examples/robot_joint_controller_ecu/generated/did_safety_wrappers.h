/*
 * =============================================================================
 * Xaloqi EDS
 * FILE: GENERATED — generated/did_safety_wrappers.h
 *
 * ECU       : RobotJointController
 * Version   : 1.0.0
 * Generated : 2026-05-20T07:21:48Z
 * ASIL Level: B
 *
 * PURPOSE: Declarations for per-DID safe accessor wrapper functions.
 *          Each function enforces the full ASIL-B validation chain before
 *          invoking the underlying DID read/write callback.
 *
 *          Architecture:
 *            diagnostics_config.yaml (dids section)
 *              -> tools/codegen.py (build_did_safety_wrappers_context)
 *                -> tools/templates/did_safety_wrappers.h.j2
 *                  -> generated/did_safety_wrappers.h  (this file)
 *
 *          This design allows service handlers (service_0x22.c, service_0x2E.c)
 *          to call a single safe accessor per DID rather than assembling the
 *          5-step validation chain inline in each handler.
 *
 * WARNING: DO NOT EDIT MANUALLY.
 *          Regenerate: python3 tools/codegen.py --config <yaml>
 *                                               --out generated/
 *                                               --safety-wrappers
 *
 * SAFETY  : All wrappers enforce the mandatory ASIL-B check sequence:
 *           1. NULL-checks all pointer arguments        (REQ-SAFE-004)
 *           2. Resolves DID in the database             (REQ-SAFE-001)
 *           3. Validates session + security access      (REQ-SAFE-002/003)
 *           4. Checks buffer length vs DID data_length  (REQ-SAFE-006)
 *           5. Invokes the DID read/write callback
 * STANDARD: MISRA C:2012 alignment intended.
 * =============================================================================
 */

/* AUTOGENERATED - DO NOT EDIT */

#ifndef DID_SAFETY_WRAPPERS_H
#define DID_SAFETY_WRAPPERS_H

#include "uds_types.h"
#include "uds_safety.h"
#include "did_database.h"

#ifdef __cplusplus
extern "C" {
#endif

/* ================================================================
 * Per-DID safe accessor declarations (ASIL-B)
 *
 * Each wrapper performs NULL checks, session, security, and
 * bounds validation before invoking the DID callback.
 * ================================================================ */

/**
 * @brief Safe read accessor for DID 0xF190 — RobotSerialNumber.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xF190 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 17 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 17).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_robotserialnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xF18C — ControllerSerialNumber.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xF18C exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 8 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 8).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_controllerserialnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xA000 — Axis0Position.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xA000 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 4 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 4).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_axis0position(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xA001 — Axis0Velocity.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xA001 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_axis0velocity(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xA002 — Axis0Torque.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xA002 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 2 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 2).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_axis0torque(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xA003 — Axis0MotorTemperature.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xA003 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_axis0motortemperature(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xA004 — Axis0StatusBitmask.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xA004 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_DEFAULT            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 1 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 1).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_axis0statusbitmask(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xA010 — Axis0PositionOffset.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xA010 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 4 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 4).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_axis0positionoffset(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xA011 — Axis0SoftLimitPositive.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xA011 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 4 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 4).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_axis0softlimitpositive(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe read accessor for DID 0xA012 — Axis0SoftLimitNegative.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xA012 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 0  (REQ-SAFE-003)
 *   - buf_len >= 4 bytes      (REQ-SAFE-006)
 *
 * @param[in]  session_ctx   Active UDS session context.
 * @param[in]  security_ctx  Active UDS security context.
 * @param[out] buf           Buffer to receive DID data.
 * @param[in]  buf_len       Capacity of buf in bytes (must be >= 4).
 * @param[out] out_len       Set to number of bytes written on success.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_read_axis0softlimitnegative(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len
);

/**
 * @brief Safe write accessor for DID 0xA010 — Axis0PositionOffset.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xA010 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 1  (REQ-SAFE-003)
 *   - len == 4 bytes exactly  (REQ-SAFE-006)
 *
 * @param[in] session_ctx   Active UDS session context.
 * @param[in] security_ctx  Active UDS security context.
 * @param[in] buf           Buffer containing new DID data (4 byte(s)).
 * @param[in] len           Number of bytes in buf.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_write_axis0positionoffset(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len
);

/**
 * @brief Safe write accessor for DID 0xA011 — Axis0SoftLimitPositive.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xA011 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 1  (REQ-SAFE-003)
 *   - len == 4 bytes exactly  (REQ-SAFE-006)
 *
 * @param[in] session_ctx   Active UDS session context.
 * @param[in] security_ctx  Active UDS security context.
 * @param[in] buf           Buffer containing new DID data (4 byte(s)).
 * @param[in] len           Number of bytes in buf.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_write_axis0softlimitpositive(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len
);

/**
 * @brief Safe write accessor for DID 0xA012 — Axis0SoftLimitNegative.
 *
 * Enforces:
 *   - NULL checks on all pointer arguments       (REQ-SAFE-004)
 *   - DID 0xA012 exists in the database    (REQ-SAFE-001)
 *   - Session >= UDS_SESSION_EXTENDED            (REQ-SAFE-002)
 *   - Security level >= 1  (REQ-SAFE-003)
 *   - len == 4 bytes exactly  (REQ-SAFE-006)
 *
 * @param[in] session_ctx   Active UDS session context.
 * @param[in] security_ctx  Active UDS security context.
 * @param[in] buf           Buffer containing new DID data (4 byte(s)).
 * @param[in] len           Number of bytes in buf.
 * @return UDS_STATUS_OK on success.
 * @return UDS_STATUS_ERR_* on any check failure.
 */
uds_status_t did_safe_write_axis0softlimitnegative(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len
);

#ifdef __cplusplus
}
#endif

#endif /* DID_SAFETY_WRAPPERS_H */
