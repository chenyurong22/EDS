/*
 * =============================================================================
 * Xaloqi EDS
 * FILE: GENERATED — generated/did_safety_wrappers.c
 *
 * ECU       : RobotJointController
 * Version   : 1.0.0
 * Generated : 2026-06-23T19:16:15Z
 * ASIL Level: B
 *
 * PURPOSE: Per-DID safe accessor implementations (ASIL-B).
 *
 *          Architecture:
 *            diagnostics_config.yaml (dids section)
 *              -> tools/codegen.py (build_did_safety_wrappers_context)
 *                -> tools/templates/did_safety_wrappers.c.j2
 *                  -> generated/did_safety_wrappers.c  (this file)
 *
 * WARNING: DO NOT EDIT MANUALLY.
 *          Regenerate: python3 tools/codegen.py --config <yaml>
 *                                               --out generated/
 *                                               --safety-wrappers
 *
 * SAFETY  : Each function:
 *   1. NULL-checks all pointer arguments (REQ-SAFE-004).
 *   2. Resolves the DID in the database (REQ-SAFE-001).
 *   3. Validates session + security access (REQ-SAFE-002/003).
 *   4. Checks buffer length against DID data_length (REQ-SAFE-006).
 *   5. Invokes the DID read/write callback.
 * STANDARD: MISRA C:2012 alignment intended.
 * =============================================================================
 */

/* AUTOGENERATED - DO NOT EDIT */

#include "did_safety_wrappers.h"
#include "did_handlers.h"

/* ================================================================
 * Per-DID safe accessor implementations (ASIL-B)
 * ================================================================ */

/* --------------------------------------------------------------------------
 * Safe read: DID 0xF190 — RobotSerialNumber
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_robotserialnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)61840U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_robotserialnumber(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xF18C — ControllerSerialNumber
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_controllerserialnumber(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)61836U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_controllerserialnumber(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xA000 — Axis0Position
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_axis0position(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)40960U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_axis0position(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xA001 — Axis0Velocity
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_axis0velocity(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)40961U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_axis0velocity(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xA002 — Axis0Torque
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_axis0torque(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)40962U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_axis0torque(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xA003 — Axis0MotorTemperature
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_axis0motortemperature(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)40963U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_axis0motortemperature(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xA004 — Axis0StatusBitmask
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_axis0statusbitmask(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)40964U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_axis0statusbitmask(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xA010 — Axis0PositionOffset
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_axis0positionoffset(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)40976U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_axis0positionoffset(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xA011 — Axis0SoftLimitPositive
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_axis0softlimitpositive(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)40977U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_axis0softlimitpositive(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe read: DID 0xA012 — Axis0SoftLimitNegative
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_read_axis0softlimitnegative(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    uint8_t                  *buf,
    uint16_t                  buf_len,
    uint16_t                 *out_len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(out_len,      "out_len"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)40978U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_READ));

    /* Step 4 — Buffer bounds check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_did_data_length(entry, buf_len));

    /* Step 5 — Invoke DID read callback */
    return did_read_axis0softlimitnegative(buf, buf_len, out_len);
}

/* --------------------------------------------------------------------------
 * Safe write: DID 0xA010 — Axis0PositionOffset
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_write_axis0positionoffset(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)40976U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_WRITE));

    /* Step 4 — Write length check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_write_data_length(entry, len));

    /* Step 5 — Invoke DID write callback */
    return did_write_axis0positionoffset(buf, len);
}

/* --------------------------------------------------------------------------
 * Safe write: DID 0xA011 — Axis0SoftLimitPositive
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_write_axis0softlimitpositive(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)40977U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_WRITE));

    /* Step 4 — Write length check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_write_data_length(entry, len));

    /* Step 5 — Invoke DID write callback */
    return did_write_axis0softlimitpositive(buf, len);
}

/* --------------------------------------------------------------------------
 * Safe write: DID 0xA012 — Axis0SoftLimitNegative
 * -------------------------------------------------------------------------- */
uds_status_t did_safe_write_axis0softlimitnegative(
    const uds_session_ctx_t  *session_ctx,
    const uds_security_ctx_t *security_ctx,
    const uint8_t            *buf,
    uint16_t                  len)
{
    const did_entry_t *entry = NULL;

    /* Step 1 — NULL checks (REQ-SAFE-004) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(session_ctx,  "session_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(security_ctx, "security_ctx"));
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_null_ptr(buf,          "buf"));

    /* Step 2 — DID lookup (REQ-SAFE-001) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_find_did((uint16_t)40978U, &entry));

    /* Step 3 — Session + security access validation (REQ-SAFE-002/003) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_validate_did_access(
        entry, session_ctx, security_ctx, UDS_SAFETY_ACCESS_WRITE));

    /* Step 4 — Write length check (REQ-SAFE-006) */
    UDS_SAFETY_RETURN_IF_ERR(uds_safety_check_write_data_length(entry, len));

    /* Step 5 — Invoke DID write callback */
    return did_write_axis0softlimitnegative(buf, len);
}
